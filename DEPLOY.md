# 🚀 Деплой на Vercel

## Быстрый старт

### 1. Подготовка

\`\`\`bash
# Убедитесь что все зависимости установлены
npm install

# Проверьте сборку
npm run build
\`\`\`

### 2. Деплой через Vercel CLI

\`\`\`bash
# Установить Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте
vercel

# Или сразу в production
vercel --prod
\`\`\`

### 3. Настройка переменных окружения

В [Vercel Dashboard](https://vercel.com) → Settings → Environment Variables добавьте:

\`\`\`
VITE_SUPABASE_URL=https://smyvnutonzuxmihmxnar.supabase.co
VITE_SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
\`\`\`

**⚠️ ВАЖНО:** Используйте `anon` ключ, не `service_role`!

### 4. Проверка

После деплоя откройте URL и проверьте:
- ✅ Страница загружается
- ✅ API работает
- ✅ Telegram Web App открывается

## Деплой через GitHub

### 1. Push в репозиторий

\`\`\`bash
git remote add origin https://github.com/tribul8ion/Telegramequipmentmanagementapp
git push -u origin main
\`\`\`

### 2. Подключить к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Выберите репозиторий `Telegramequipmentmanagementapp`
4. Настройте переменные окружения (см. выше)
5. Нажмите "Deploy"

### 3. Автоматические деплои

После этого каждый push в `main` будет автоматически деплоить приложение!

## Настройка BotFather

После получения URL от Vercel, настройте бота:

\`\`\`
/setmenubutton
@your_bot_name
Открыть приложение
https://your-project.vercel.app
\`\`\`

## Troubleshooting

### CORS ошибки

Если видите CORS ошибки, проверьте:
1. RLS политики в Supabase настроены
2. `anon` ключ используется (не `service_role`)
3. URL корректный

### Сборка падает

Проверьте:
1. Node.js версия 18+
2. Все зависимости установлены
3. Нет синтаксических ошибок

### Web App не открывается в Telegram

Проверьте:
1. URL в BotFather правильный
2. HTTPS работает (Vercel дает автоматически)
3. Бот авторизован

---

Готово! 🎉
